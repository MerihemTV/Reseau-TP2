#include "player.hpp"

/*
Player::Player(float x, float y, float z, std::string name, float rotx, float roty, float rotz, float rotw) :
	m_posX(x),
	m_posY(y),
	m_posZ(z),
	m_name(name),
	m_rotX(rotx),
	m_rotY(roty),
	m_rotW(rotw),
	m_rotZ(rotz) {}*/